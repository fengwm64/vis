# QA Report: Dijkstra 最短路径可视化

Issue: #18
Pipeline: auto-dev
Slug: `dijkstra`
Date: 2026-05-02

## 1. 构建结果

**PASS** - `npm run build` 通过，无错误、无警告。

```
✓ 409 modules transformed.
✓ built in 2.07s
```

## 2. 算法测试结果

**PASS** - `runAlgorithmTests()` 全部通过。

```
All Dijkstra algorithm tests passed.
```

测试覆盖：默认图最短路径、路径重建（prev）、初始步骤、visited 集合单调递增、单节点图、线性图、断开图、步骤数范围、步骤字段完整性。

## 3. PRD 验收清单

| # | 验收项 | 结果 |
|---|--------|------|
| 1 | `npm run build` 通过 | ✅ PASS |
| 2 | 算法自检通过 | ✅ PASS |
| 3 | 算法模块为纯函数，零 DOM/React 依赖 | ✅ PASS |
| 4 | 导出 `computeSteps(graph, source)` | ✅ PASS |
| 5 | 导出 `runAlgorithmTests()` | ✅ PASS |
| 6 | `meta.js` 导出 `category: "graph"` | ✅ PASS |
| 7 | 页面自动出现在首页图算法分组 | ✅ PASS（Vite import.meta.glob 自动发现） |
| 8 | 图形可视化清晰展示节点、带权边、距离表 | ✅ PASS |
| 9 | 播放/暂停/单步/重置控件正常工作 | ✅ PASS |
| 10 | 每一步都有文字说明当前操作 | ✅ PASS |
| 11 | 已确定节点和待处理节点有明显视觉区分 | ✅ PASS（绿色 vs 白色，靛蓝色当前） |
| 12 | 松弛操作时高亮对应边 | ✅ PASS（紫色闪烁动画） |
| 13 | 最短路径树逐步构建并可视化 | ✅ PASS（绿色边 + 路径面板） |
| 14 | 卡片内容区域保留顶部留白 | ✅ PASS |
| 15 | 无无用/重复/死按钮 | ✅ PASS |
| 16 | 自动播放定时器在组件卸载时正确清理 | ✅ PASS |

## 4. UI 控件审计

| 检查项 | 结果 |
|--------|------|
| 每个按钮有明确用途 | ✅ 播放/暂停、下一步、重置 — 各有独立功能 |
| 无无用按钮 | ✅ 所有 3 个按钮均有实际作用 |
| 无重复按钮 | ✅ 控件语义不重复 |
| 无死按钮 | ✅ 所有按钮可触发状态变化 |
| 禁用态合理 | ✅ 无永久禁用按钮 |
| 控件文案与行为一致 | ✅ "播放"/"暂停" 切换正确，"下一步" 前进一步，"重置" 回到初始 |

## 5. 交互 bug 审计

| 检查项 | 结果 |
|--------|------|
| 播放/暂停 | ✅ 切换正常，自动播放到末尾自动停止 |
| 单步前进 | ✅ 每次推进一步 |
| 重置 | ✅ 回到步骤 0，停止播放 |
| 边界：步骤 0 | ✅ 重置在步骤 0 无副作用 |
| 边界：最后一步 | ✅ 自动播放停止，单步不越界（Math.min 保护） |
| 自动播放定时器 | ✅ useEffect 返回 cleanup，无泄漏 |
| 路由 | ✅ `/animations/dijkstra` 路由正常 |
| 响应式布局 | ✅ `lg:grid-cols-[1.35fr_1fr]` 桌面双栏，移动端单栏 |

## 6. 卡片布局留白审计

| 卡片 | padding 类 | 有顶部留白 | 结果 |
|------|-----------|-----------|------|
| 带权有向图 | `p-4 pt-4` | ✅ | PASS |
| 当前操作 | `p-5 pt-5` | ✅ | PASS |
| 距离表 | `p-5 pt-5` | ✅ | PASS |
| 最短路径树 | `p-5 pt-5` | ✅ | PASS |
| 读图方式 | `p-5 pt-5` | ✅ | PASS |

无 `pt-0`、`!pt-0` 或 `padding-top: 0` 出现。

## 7. 备注

- PRD 示例中 A→F 最短路径写为 9（A→B→D→F），实际应为 11（A→B→D→F = 4+5+2 = 11，或 A→B→F = 4+7 = 11）。算法实现正确，PRD 文档有误，不影响代码质量。
- 控件模式与 PageRank 动画保持一致（PRD 要求"参照 PageRank 动画模式"）。

## 8. 结论

**QA PASS** — 全部验收项通过，无阻塞缺陷。
