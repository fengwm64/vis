# QA Report: Issue #16

## 构建结果

- `npm run build`: ✅ 通过（2.29s，406 modules）

## 算法测试结果

- `runAlgorithmTests()`: ✅ 所有断言通过

## PRD 验收清单

| # | 验收项 | 结果 |
|---|--------|------|
| 1 | 点击"随机生成"按钮 10 次，生成的数组不包含任何负数（所有值 >= 1） | ✅ 通过 — `randomArray` 使用 `Math.floor(Math.random() * 99) + 1`，范围 [1, 99] |
| 2 | 随机生成的数组仍能正确排序和动画播放 | ✅ 通过 — 算法测试通过，build 正常 |
| 3 | 手动输入含负数的数组仍能正常排序和可视化（零基线逻辑保留） | ✅ 通过 — 零基线渲染逻辑（第 188-195 行）保留未删除 |
| 4 | `npm run build` 通过 | ✅ 通过 |
| 5 | 无新增无用按钮、重复按钮或文案不一致 | ✅ 通过 — 未新增任何 UI 元素 |

## UI 控件审计

| 检查项 | 结果 |
|--------|------|
| 每个按钮有明确用途 | ✅ 播放/暂停、上一步、下一步、重置、随机生成，均有对应行为 |
| 无用/重复/占位/死按钮 | ✅ 无 |
| 控件文案与行为一致 | ✅ "播放"切换为播放态，"暂停"切换为暂停态，语义正确 |
| 禁用态合理 | ✅ 无永久禁用按钮 |

## 交互 bug 审计

| 检查项 | 结果 |
|--------|------|
| 播放/暂停 | ✅ 正确切换 playing 状态 |
| 单步前进/后退 | ✅ goNext/goPrev 边界正确（Math.min/Math.max） |
| 重置 | ✅ 回到 stepIndex=0，停止播放 |
| 边界步骤 | ✅ 第 0 步不能后退，最后一步自动停止播放 |
| 自动播放定时器 | ✅ 使用 setTimeout，组件卸载时通过 useEffect cleanup 清除 |
| 路由和响应式 | ✅ 布局使用 grid + flex，适配移动端和桌面端 |

## 卡片布局留白审计

| 检查项 | 结果 |
|--------|------|
| CardContent padding | ✅ 使用 `p-4` 或 `p-5`，无 `pt-0` / `padding-top: 0` |
| 顶部留白 | ✅ 所有卡片均有正常顶部 padding |

## 结论

修复方案简洁正确：仅修改 `randomArray` 函数的数值范围从 `[-99, 98]` 到 `[1, 99]`，无多余改动。负数可视化逻辑保留，手动输入负数仍可正常工作。所有验收项通过，无 UI/交互缺陷。
