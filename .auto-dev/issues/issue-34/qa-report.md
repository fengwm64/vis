# QA Report: Issue #34 - 快速排序随机生成数组不应允许负数

## 构建结果

- `npm run build`: **通过** (2.33s, 415 modules)

## 算法测试结果

- `runAlgorithmTests()`: **通过** (所有 12 组断言通过)

## PRD 验收清单

| # | 验收项 | 结果 |
|---|--------|------|
| 1 | 点击"随机生成"按钮后，数组中所有值均为正整数（≥ 1） | **通过** — `randomArray` 改为 `Math.floor(Math.random() * 99) + 1`，范围 [1, 99] |
| 2 | 柱状图正常显示，无向下延伸的异常柱条 | **通过** — 随机数组全为正数，柱条均向上延伸 |
| 3 | 排序动画正常运行，步骤说明正确 | **通过** — 算法测试覆盖默认数组、已排序、逆序、重复值等场景 |
| 4 | 用户手动输入负数时仍可正常排序 | **通过** — `parseInput` 逻辑未修改，仍接受负数；`computeSteps` 测试 5 验证负数排序正确 |
| 5 | `npm run build` 构建通过 | **通过** |
| 6 | 无新增按钮、无删除已有功能 | **通过** — 仅修改 `randomArray` 函数，控件未变 |

## UI 控件审计

| 检查项 | 结果 |
|--------|------|
| 每个按钮有明确用途 | **通过** — 播放/暂停、上一步、下一步、重置、随机生成，共 5 个按钮，均有明确功能 |
| 无用/占位/死按钮 | **通过** — 无 |
| 重复按钮 | **通过** — 无语义重复 |
| 禁用态合理性 | **通过** — 边界步骤由 `Math.min`/`Math.max` 钳制，不依赖禁用态 |
| 控件文案与行为一致 | **通过** — "播放"切换播放、"暂停"暂停、"上一步"/"下一步"单步移动、"重置"回到第 0 步 |

## 交互 Bug 审计

| 检查项 | 结果 |
|--------|------|
| 播放/暂停 | **通过** — `playing` 状态正确切换 |
| 单步前进/后退 | **通过** — `goNext`/`goPrev` 边界钳制正确 |
| 重置 | **通过** — 回到 step 0 并停止播放 |
| 边界步骤（第 0 步后退、最后一步前进） | **通过** — `Math.max(0, ...)` 和 `Math.min(..., steps.length-1)` |
| 自动播放定时器 | **通过** — 使用 `setTimeout` + `clearTimeout` 清理，到达末步自动停止，无泄漏 |
| 路由和响应式布局 | **通过** — 页面独立，响应式 `grid` 和 `flex` 布局合理 |

## 卡片布局留白审计

| 检查项 | 结果 |
|--------|------|
| `CardContent` 是否有顶部留白 | **通过** — 所有 `CardContent` 使用 `p-4` 或 `p-5`，无 `pt-0` |
| 控制卡片/说明卡片留白 | **通过** — 统计信息、算法说明、输入控制、速度控制卡片均使用 `p-5` |
| 是否存在 `pt-0` / `!pt-0` / `padding-top: 0` | **通过** — 未发现 |

## 结论

**全部通过。** 修复仅将 `randomArray` 的范围从 `[-99, 98]` 改为 `[1, 99]`，消除了随机生成数组中的负数，柱状图可视化不再出现布局异常。算法本身不受影响，手动输入负数仍可正常排序。
